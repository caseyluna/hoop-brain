version: "3"

vars:
  DAGGER_SCRIPT: "infra/dagger/run_ci_pipeline.py"
  DAGGER_RUN: "dagger run python {{.DAGGER_SCRIPT}}"

includes:
  ingestion:
    taskfile: ./backend/ingestion-engine/Taskfile.yml
    dir: ./backend/ingestion-engine

  model:
    taskfile: ./backend/model-engine/Taskfile.yml
    dir: ./backend/model-engine

  transformation:
    taskfile: ./backend/transformation-engine/Taskfile.yml
    dir: ./backend/transformation-engine

tasks:
  all-checks:
    desc: Run all checks on all services sequentially
    cmds:
      - task ingestion:all-checks
      - task model:all-checks
      - task transformation:all-checks
