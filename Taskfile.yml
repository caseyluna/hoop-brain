tasks:
  ingestion:all:
    desc: Run all checks for ingestion-engine
    cmds:
      - task --dir backend/ingestion-engine all

  all:code-checks:
    desc: Run all services' checks
    cmds:
      - task ingestion:all
      - task model:all
      - task dbt:all
