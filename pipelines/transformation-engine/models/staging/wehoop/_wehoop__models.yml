version: 2

models:
  - name: stg_wehoop__teams
    description: Deduped WNBA team records staged from raw_wehoop.teams.
    columns:
      - name: id
        description: ESPN WNBA team ID (primary key).
        tests:
          - unique
          - not_null
      - name: league
        description: League the team belongs to. Always 'WNBA' for this source.
        tests:
          - not_null
