[project]
name = "transformation-engine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "anyio>=4.5.2",
    "dbt-bigquery>=1.8.3",
    "dbt-core>=1.8.7",
]
