# pipelines.yaml
pipelines:
  lint-all:
    steps:
      - api: [lint]
      - sync-engine: [lint]
      - web: [lint]

  typecheck-all:
    steps:
      - api: [typecheck]
      - sync-engine: [typecheck]
      - web: [typecheck]

  coverage-all:
    steps:
      - api: [coverage]
      - sync-engine: [coverage]

  test-all:
    steps:
      - api: [test]
      - sync-engine: [test]
      - web: [test]

  integration-test:
    steps:
      - api: [migrate-test]
      - sync-engine: [run-main]
      - api: [integration-test]

  ci-all:
    steps:
      - api: [lint, typecheck, test, coverage]
      - sync-engine: [lint, typecheck, test, coverage]
      - web: [lint, typecheck, test]
